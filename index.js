const names = ["Ada", "Brendan", "Ali"]
const eventt = ["for the wonderful prayer!"];

function writeCards(namess, eventtt){
  for(i = 0; i < namess.length; i++){
    
   } let arrayy =[`Thank you, ${namess[i]} for the ${eventtt}`]
} writeCards(names, eventt)